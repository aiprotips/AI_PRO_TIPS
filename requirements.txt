# HTTP client per API-Football
requests==2.31.0

# Parsing date/time dalle API
python-dateutil==2.9.0.post0
pytz==2024.1

# Telegram Bot API (long polling)
python-telegram-bot==13.15

# Database (MySQL/MariaDB)
SQLAlchemy==2.0.23
PyMySQL==1.1.0

# Utility extra
alembic==1.13.1   # per migrazioni future (opzionale, se usi 001_init.sql basta SQLAlchemy)
